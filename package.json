{
  "homepage": "https://rikulauttia.com",
  "name": "Riku Lauttia",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "autoprefixer": "^10.4.20",
    "next": "^16.1.1",
    "postcss": "^8.4.45",
    "react": "^19.2.3",
    "react-dom": "^19.2.3",
    "tailwindcss": "^3.4.11"
  },
  "scripts": {
    "gen:og": "node scripts/generate-og.mjs",
    "gen:sitemap": "node scripts/generate-sitemap.mjs",
    "prebuild": "npm run gen:og && npm run gen:sitemap",
    "dev": "next dev",
    "build": "next build",
    "postbuild": "touch out/.nojekyll",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d out -t true"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "sharp": "^0.34.5"
  }
}
